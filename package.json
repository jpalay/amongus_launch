{
  "name": "amongus_launch",
  "version": "1.0.0",
  "description": "launch button",
  "main": "out/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "watch": "watch 'npm run build' ./src"
  },
  "author": "Josh Palay",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "npm-watch": "^0.7.0",
    "watch": "^1.0.2"
  }
}
